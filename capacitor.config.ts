import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.card.learner.app',
  appName: 'Card Learner',
  webDir: 'dist/card-learner',
  bundledWebRuntime: false
};

export default config;
